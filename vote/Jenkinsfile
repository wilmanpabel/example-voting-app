pipeline {
    agent {
        docker {
            image 'node:latest'
            }
    }

    stages {
        stage('build') {
            when {
                changeset "**/result/**"
            }
            steps {
                echo "compiling"
                dir('vote'){
                    sh 'ls'
                }
            }
        }
    }

}
